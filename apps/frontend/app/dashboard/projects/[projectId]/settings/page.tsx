"use client";

import type { ReactElement } from "react";

import { SettingsTabPanel } from "@/components/project-workspace/project-workspace-tabs-extra";

export default function ProjectSettingsPage(): ReactElement {
  return <SettingsTabPanel />;
}
